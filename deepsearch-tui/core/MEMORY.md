deepsearch-tui 项目状态 (2026-07-12 最新): 路径 /home/ivan/Projects/deepsearch-tui，GitHub BobDylans/deepsearch-tui（已推送 HTTPS，SSH key 未配）。技术栈 LangGraph 1.2+ + LangChain 1.3+ create_agent + ChromaDB + Textual TUI。Phase 1-7 全部完成 + pi-inspired 5 项改进。Phase 3: middleware 链（ThinkingMiddleware/Analytics/OutputLogging）+ custom 事件。Phase 5A: 会话持久化（SessionStore JSONL 格式 ~/.local/share/deepsearch-tui/sessions.json）+ 断线重连。Phase 5B: 滑动窗口记忆（ConversationSummarizer，window_size=10, summary_threshold=12）+ context_loader 节点（START→context_loader→main→END）。Phase 6: TUI 斜杠命令（/index /collections /delete /help）+ PDF 可选依赖。Phase 7: 已推送 GitHub。TUI 美化：模仿 pi zentui 极简风格，Tokyo Night 配色，无边框对话区，对话区:侧栏=5:2。pi-inspired 改进全部完成：①流式渲染缓冲区替换式更新（_entries + clear/rewrite）②工具调用三态 Panel（pending黄/success绿/error红）③错误重试（retry.py，指数退避，is_retryable_error 正则匹配）④增量摘要更新（_update_summary，窗口滑动时增量 LLM 摘要）⑤会话 JSONL 持久化（向后兼容旧 JSON）。一键启动脚本 start.sh（自动管理后端+TUI）。.env.base 用 enncloud 网关（fastai.enncloud.cn/v1, key sk-ij3gSzLS-..., glm-5.2-fast，之前用 qwen3.7-plus-fast 太慢改为 glm-5.2-fast）。已知问题：①langgraph dev server 的 context_loader 节点不通过 API server HTTP 执行（graph 加载机制问题，直接 graph.ainvoke 正常；根因是 langgraph_api 的 _graph_from_spec 用 spec_from_file_location+exec_module 加载 entry.py，但 server 进程中 entry.py 模块级代码未执行，API server 用的 graph 没有 context_loader 节点）②LLM 摘要调用在 context_loader 中可能阻塞事件循环（已加 15s asyncio.wait_for + asyncio.to_thread + model.invoke 替代 ainvoke 包装）③structlog ConsoleRenderer API 变化（_level_styles_default → get_default_level_styles()）④Textual @work 装饰器不能和 run_worker 同时用（双重包装导致 WorkerError: Unsupported attempt to run an async worker）⑤LangGraph messages/partial 事件发的是累积快照非 delta，render_ai_chunk 需用 diff/替换模式。⑥context_loader 的 ConversationSummarizer 调 model.ainvoke() 会阻塞 LangGraph server 事件循环导致 server 无法响应任何请求（TUI 报 Connection error），已改用 asyncio.to_thread(model.invoke) + asyncio.wait_for(timeout=15s) 修复。 <!-- created=2026-07-10, last=2026-07-11 -->